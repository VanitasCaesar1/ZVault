---
const steps = [
  {
    num: '1',
    title: 'Import',
    desc: 'zvault import .env encrypts your secrets locally and generates safe zvault:// references.',
    icon: '<path d="M12 3v12m0 0l-4-4m4 4l4-4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M4 17h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>',
  },
  {
    num: '2',
    title: 'Code with AI',
    desc: 'Your AI assistant sees references, not values. It knows what secrets exist without seeing them.',
    icon: '<path d="M9.75 3.104v5.714a2.25 2.25 0 01-.659 1.591L5 14.5M9.75 3.104c-.251.023-.501.05-.75.082m.75-.082a24.301 24.301 0 014.5 0m0 0v5.714a2.25 2.25 0 00.659 1.591L19 14.5M14.25 3.104c.251.023.501.05.75.082M5 14.5l-1.43 1.43a1.5 1.5 0 001.06 2.57h14.74a1.5 1.5 0 001.06-2.57L19 14.5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>',
  },
  {
    num: '3',
    title: 'Push to Cloud',
    desc: 'zvault cloud push syncs secrets to all environments — dev, staging, production. One source of truth.',
    icon: '<path d="M12 16V3m0 0l-4 4m4-4l4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M2 15c6.667-6 13.333 0 20-6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>',
  },
  {
    num: '4',
    title: 'Deploy',
    desc: 'Your app fetches secrets at runtime from ZVault Cloud via SDK or CLI. No AWS Secrets Manager needed.',
    icon: '<path d="M5 3l14 9-14 9V3z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>',
  },
];
---

<section class="py-24">
  <div class="container">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold tracking-tight mb-4">Dev to prod. Four commands.</h2>
      <p class="text-muted text-lg max-w-xl mx-auto">From local .env to production deployment — one tool, every environment. No AWS Secrets Manager needed.</p>
    </div>

    <div class="grid md:grid-cols-4 gap-8 max-w-5xl mx-auto relative">
      <!-- Connecting line (desktop) -->
      <div class="hidden md:block absolute top-12 left-[12.5%] right-[12.5%] h-px bg-gradient-to-r from-accent/0 via-accent/30 to-accent/0"></div>

      {steps.map((step, i) => (
        <div class="text-center relative" data-animate>
          <div class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-accent-dim border border-accent/20 text-accent font-bold text-lg mb-5 relative z-10">
            {step.num}
          </div>
          <h3 class="text-lg font-semibold mb-2">{step.title}</h3>
          <p class="text-muted text-sm leading-relaxed">{step.desc}</p>
        </div>
      ))}
    </div>

    <!-- Time indicator + CTA -->
    <div class="text-center mt-12 space-y-4">
      <div class="inline-flex items-center gap-2 text-[13px] text-dim">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
        Total setup time: ~2 minutes
      </div>
      <div>
        <a href="https://docs.zvault.cloud/getting-started/quickstart" class="inline-flex items-center gap-2 px-5 py-2.5 rounded-xl bg-accent text-bg font-semibold text-sm hover:bg-accent/90 transition-all hover:shadow-[0_0_20px_rgba(34,211,238,0.15)]">
          Follow the Quick Start
          <svg width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10m0 0L9 4m4 4L9 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
      </div>
    </div>
  </div>
</section>
