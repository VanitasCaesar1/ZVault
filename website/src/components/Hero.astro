---
---

<section class="relative pt-32 pb-20 overflow-hidden">
  <!-- Background grid -->
  <div class="absolute inset-0 opacity-[0.03]" style="background-image: linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px); background-size: 60px 60px;"></div>

  <!-- Gradient orbs -->
  <div class="absolute top-20 left-1/4 w-[500px] h-[500px] bg-accent/5 rounded-full blur-[120px] animate-pulse-glow"></div>
  <div class="absolute top-40 right-1/4 w-[400px] h-[400px] bg-purple/5 rounded-full blur-[120px] animate-pulse-glow" style="animation-delay: 1.5s;"></div>

  <div class="container relative z-10 text-center">
    <!-- Problem agitation — make the pain real -->
    <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-red/30 bg-red/5 text-xs text-red/90 mb-6 animate-fade-in">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
      Your secrets are scattered across .env files, AWS SM, and 5 dashboards
    </div>

    <!-- Headline -->
    <h1 class="text-4xl sm:text-5xl md:text-7xl font-extrabold tracking-tight leading-[1.05] max-w-4xl mx-auto mb-6 animate-fade-in-up" style="animation-delay: 0.1s;">
      One vault. Every environment.<br/><span class="gradient-text">AI-native.</span>
    </h1>

    <!-- Subtext -->
    <p class="text-lg md:text-xl text-muted max-w-2xl mx-auto mb-10 leading-relaxed animate-fade-in-up" style="animation-delay: 0.2s;">
      ZVault manages your secrets from local dev to production. Your AI sees <code class="text-purple font-mono text-base">zvault://</code> references — never real values. Your app fetches secrets at runtime from ZVault Cloud. No AWS Secrets Manager. No scattered env vars.
    </p>

    <!-- CTAs -->
    <div class="flex items-center justify-center gap-4 flex-wrap mb-8 animate-fade-in-up" style="animation-delay: 0.3s;">
      <a href="https://docs.zvault.cloud/getting-started/quickstart" class="group inline-flex items-center gap-2 px-6 py-3 rounded-xl bg-accent text-bg font-semibold hover:bg-accent/90 transition-all hover:shadow-[0_0_30px_rgba(34,211,238,0.2)]">
        Start Free (Local)
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" class="transition-transform group-hover:translate-x-0.5"><path d="M3 8h10m0 0L9 4m4 4L9 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
      <a href="https://app.zvault.cloud" class="inline-flex items-center gap-2 px-6 py-3 rounded-xl bg-bg-card border border-accent/30 text-accent font-semibold hover:bg-accent/5 hover:border-accent/50 transition-all">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M2 15c6.667-6 13.333 0 20-6"/><path d="M9 22c1.798-4.56 3.1-8.272 6-12"/><path d="M15 2c-1.798 4.56-3.1 8.272-6 12"/></svg>
        Try Cloud
      </a>
    </div>

    <!-- Install box -->
    <div class="inline-flex items-center gap-3 bg-bg-card border border-border rounded-xl px-4 sm:px-5 py-3 max-w-full overflow-x-auto animate-fade-in-up" style="animation-delay: 0.4s;">
      <span class="text-dim text-sm shrink-0">$</span>
      <code class="text-accent font-mono text-xs sm:text-sm whitespace-nowrap">curl -fsSL https://zvault.cloud/install.sh | sh</code>
      <button id="copy-install" class="text-dim hover:text-white transition-colors p-1 shrink-0" aria-label="Copy install command">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
          <rect x="5" y="5" width="9" height="9" rx="2"/>
          <path d="M3 11V3a2 2 0 012-2h8"/>
        </svg>
      </button>
    </div>

    <!-- Micro trust signals under install -->
    <div class="flex items-center justify-center gap-3 sm:gap-4 mt-5 text-[11px] text-dim flex-wrap animate-fade-in-up" style="animation-delay: 0.5s;">
      <span class="flex items-center gap-1">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
        No account required
      </span>
      <span class="flex items-center gap-1">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
        Dev to prod
      </span>
      <span class="flex items-center gap-1">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
        Replaces AWS SM
      </span>
      <span class="flex items-center gap-1">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#4ade80" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
        2 min setup
      </span>
    </div>
  </div>
</section>

<script>
  document.getElementById('copy-install')?.addEventListener('click', async () => {
    await navigator.clipboard.writeText('curl -fsSL https://zvault.cloud/install.sh | sh');
    const btn = document.getElementById('copy-install');
    if (btn) {
      btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="#4ade80" stroke-width="2"><path d="M3 8l3 3 7-7"/></svg>';
      setTimeout(() => {
        btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="5" y="5" width="9" height="9" rx="2"/><path d="M3 11V3a2 2 0 012-2h8"/></svg>';
      }, 2000);
    }
  });
</script>
