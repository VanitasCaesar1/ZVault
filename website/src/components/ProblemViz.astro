---
// Problem visualization — show developers exactly what their AI sees
// This creates the "oh no" moment that makes the solution click
---

<section class="py-16">
  <div class="container">
    <div class="text-center mb-10">
      <h2 class="text-2xl md:text-3xl font-bold tracking-tight mb-3">What your AI assistant sees right now</h2>
      <p class="text-muted text-base max-w-lg mx-auto">Every file in your project is context for AI tools. Including your secrets.</p>
    </div>

    <div class="grid md:grid-cols-2 gap-4 max-w-4xl mx-auto">
      <!-- Without ZVault -->
      <div class="relative">
        <div class="absolute -top-3 left-4 px-2.5 py-0.5 bg-red/10 border border-red/20 rounded-full text-[10px] text-red font-medium z-10">
          Without ZVault
        </div>
        <div class="bg-bg-card border border-red/20 rounded-2xl p-5 h-full">
          <div class="flex items-center gap-2 mb-3 text-xs text-dim font-mono">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><path d="M14 2v6h6"/></svg>
            .env
          </div>
          <div class="font-mono text-xs space-y-1.5">
            <div><span class="text-dim">STRIPE_KEY=</span><span class="text-red">sk_live_51J3xKpQR7mN8vWZ</span></div>
            <div><span class="text-dim">DB_URL=</span><span class="text-red">postgres://admin:s3cret@db:5432</span></div>
            <div><span class="text-dim">AWS_SECRET=</span><span class="text-red">wJalrXUtnFEMI/K7MDENG</span></div>
            <div><span class="text-dim">JWT_SECRET=</span><span class="text-red">super-secret-jwt-key-2024</span></div>
          </div>
          <div class="mt-4 pt-3 border-t border-red/10">
            <div class="flex items-center gap-1.5 text-[10px] text-red/70">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M15 9l-6 6M9 9l6 6"/></svg>
              AI can read, autocomplete, and leak these values
            </div>
          </div>
        </div>
      </div>

      <!-- With ZVault -->
      <div class="relative">
        <div class="absolute -top-3 left-4 px-2.5 py-0.5 bg-green/10 border border-green/20 rounded-full text-[10px] text-green font-medium z-10">
          With ZVault
        </div>
        <div class="bg-bg-card border border-green/20 rounded-2xl p-5 h-full">
          <div class="flex items-center gap-2 mb-3 text-xs text-dim font-mono">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><path d="M14 2v6h6"/></svg>
            .env.zvault
          </div>
          <div class="font-mono text-xs space-y-1.5">
            <div><span class="text-dim">STRIPE_KEY=</span><span class="text-purple">zvault://env/prod/STRIPE_KEY</span></div>
            <div><span class="text-dim">DB_URL=</span><span class="text-purple">zvault://env/prod/DB_URL</span></div>
            <div><span class="text-dim">AWS_SECRET=</span><span class="text-purple">zvault://env/prod/AWS_SECRET</span></div>
            <div><span class="text-dim">JWT_SECRET=</span><span class="text-purple">zvault://env/prod/JWT_SECRET</span></div>
          </div>
          <div class="mt-4 pt-3 border-t border-green/10">
            <div class="flex items-center gap-1.5 text-[10px] text-green/70">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
              AI sees references only — values resolved at runtime
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
