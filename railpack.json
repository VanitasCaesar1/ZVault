{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "...",
        "cargo build --release --bin zvault-server"
      ]
    }
  },
  "deploy": {
    "startCommand": "./bin/zvault-server"
  }
}
