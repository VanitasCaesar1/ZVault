[project]
name = "django-zvault"
version = "0.1.0"
description = "ZVault integration for Django — settings.py secret injection"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = ["httpx>=0.25"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
