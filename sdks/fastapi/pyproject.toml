[project]
name = "fastapi-zvault"
version = "0.1.0"
description = "ZVault integration for FastAPI — dependency injection for secrets"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = ["httpx>=0.25"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
