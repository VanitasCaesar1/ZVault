[build]
builder = "RAILPACK"

[build.buildCommand]
command = "cargo build --release --bin zvault-server"

[deploy]
startCommand = "./target/release/zvault-server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
