name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: zvault-${{ github.ref_name }}-linux-x86_64.tar.gz
            use_cross: false
            extra_features: ""
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            archive: zvault-${{ github.ref_name }}-linux-aarch64.tar.gz
            use_cross: true
            extra_features: --features zvault-cli/vendored-openssl
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: zvault-${{ github.ref_name }}-darwin-x86_64.tar.gz
            use_cross: false
            extra_features: ""
          - target: aarch64-apple-darwin
            os: macos-14
            archive: zvault-${{ github.ref_name }}-darwin-aarch64.tar.gz
            use_cross: false
            extra_features: ""

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use_cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Install system dependencies (Linux native)
        if: startsWith(matrix.os, 'ubuntu') && !matrix.use_cross
        run: |
          sudo apt-get update
          sudo apt-get install -y libclang-dev cmake llvm

      - name: Build CLI
        run: |
          ${{ matrix.use_cross && 'cross' || 'cargo' }} build --release --package zvault-cli --target ${{ matrix.target }} ${{ matrix.extra_features }}

      - name: Build server
        run: |
          ${{ matrix.use_cross && 'cross' || 'cargo' }} build --release --package zvault-server --target ${{ matrix.target }}

      - name: Package
        run: |
          mkdir -p staging
          cp target/${{ matrix.target }}/release/zvault staging/
          cp target/${{ matrix.target }}/release/zvault-server staging/
          cp README.md staging/
          cp LICENSE* staging/ 2>/dev/null || true
          cd staging
          tar czf ../${{ matrix.archive }} .
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: ${{ matrix.archive }}

  release:
    name: Create Release
    needs: build
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Collect artifacts
        run: |
          mkdir -p dist
          find artifacts -name '*.tar.gz' -type f -exec cp {} dist/ \;
          ls -la dist/

      - name: Generate checksums
        run: |
          cd dist
          sha256sum *.tar.gz > checksums-sha256.txt
          cat checksums-sha256.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body_path: .github/release-notes.md
          append_body: true
          files: |
            dist/*.tar.gz
            dist/checksums-sha256.txt

  publish-crates:
    name: Publish to crates.io
    needs: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Publish zvault-storage
        run: cargo publish --package zvault-storage --no-verify
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        continue-on-error: true

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish zvault-core
        run: cargo publish --package zvault-core --no-verify
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        continue-on-error: true

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish zvault-server
        run: cargo publish --package zvault-server --no-verify
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        continue-on-error: true

      - name: Wait for crates.io index
        run: sleep 30

      - name: Publish zvault-cli
        run: cargo publish --package zvault-cli --no-verify
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        continue-on-error: true

  publish-npm:
    name: Publish to npm
    needs: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Update npm package version
        run: |
          TAG="${{ github.ref_name }}"
          VERSION="${TAG#v}"
          cd npm
          npm version "$VERSION" --no-git-tag-version --allow-same-version

      - name: Publish
        run: npm publish --access public
        working-directory: npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: true
