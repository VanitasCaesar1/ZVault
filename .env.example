# ZVault Server Configuration
# Copy to .env and adjust as needed

# Bind address (PORT takes precedence for Railway/Render)
# PORT=8200
VAULTRS_BIND_ADDR=127.0.0.1:8200

# Storage backend: memory, rocksdb, redb
VAULTRS_STORAGE=memory
VAULTRS_STORAGE_PATH=./data

# Logging: debug, info, warn, error
VAULTRS_LOG_LEVEL=info

# Audit log file (optional, leave empty to disable file audit)
# VAULTRS_AUDIT_FILE=./audit/vault.log

# Disable mlockall (set to true in containers without CAP_IPC_LOCK)
VAULTRS_DISABLE_MLOCK=false

# Dashboard URL (separate service in production)
# DASHBOARD_URL=https://app.zvault.cloud

# Docs URL (separate service in production)
# DOCS_URL=https://docs.zvault.cloud

# ── Dashboard (.env in dashboard/) ──────────────────────────────────
# VITE_API_URL=http://localhost:8200
