[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run build"

[deploy]
# Vite SPA — serve built static files with a lightweight server.
startCommand = "npx serve dist --listen ${PORT:-3000} --single"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
