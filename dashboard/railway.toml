[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "node_modules/.bin/serve dist --listen ${PORT:-3000} --single"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
